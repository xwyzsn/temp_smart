### INSTRUCTIONS
1. Download the `local_(1).ipynb` file (it should be included in the repository; if not, ensure it's present).
2. Launch Jupyter Notebook.
3. Open `local_(1).ipynb` from the file browser.
4. Run all cells sequentially by clicking "Run All" or executing them one by one (Shift + Enter).
